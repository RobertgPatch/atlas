import { AlertTriangle, CheckCircle2 } from 'lucide-react'
import type { K1TrackerYearDetail } from '../../../../../packages/types/src/k1-tracker'
export function YearStatusPanel({ detail }: { detail: K1TrackerYearDetail }) { const attention = detail.calculation.checks.filter((check) => check.status !== 'PASS'); const documents = [...new Set(detail.values.map((value) => value.sourceK1DocumentId).filter((id): id is string => Boolean(id)))]; return <div className="rounded-xl border border-gray-200 bg-white p-4 shadow-sm"><div className="flex items-center justify-between"><h3 className="text-sm font-semibold text-gray-950">Year status</h3><span className={`rounded-full px-2 py-1 text-xs font-semibold ${attention.length ? 'bg-amber-100 text-amber-800' : 'bg-emerald-100 text-emerald-800'}`}>{detail.status.replaceAll('_', ' ')}</span></div><div className="mt-3 space-y-2">{attention.length ? attention.map((check) => <div key={check.key} className="flex gap-2 rounded-md bg-amber-50 p-2 text-xs text-amber-900"><AlertTriangle className="h-4 w-4 shrink-0" />{check.message}</div>) : <p className="flex gap-2 text-sm text-emerald-700"><CheckCircle2 className="h-4 w-4" />All calculation checks pass.</p>}{detail.sourceConflicts.map((conflict) => <p key={conflict.fieldKey} className="rounded-md bg-red-50 p-2 text-xs text-red-800">{conflict.message}</p>)}{documents.map((id) => <a key={id} href={`/k1/${id}/review`} className="block text-xs font-medium text-blue-700 underline">Open finalized K-1 source</a>)}</div></div> }
