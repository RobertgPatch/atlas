import { X } from 'lucide-react'
import { useMemo, useState } from 'react'
import type { K1TrackerYearSummary } from '../../../../../packages/types/src/k1-tracker'
const currency = (value: string | null) => value == null ? '—' : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(Number(value))
export function CompareYearsDrawer({ years, selectedYear, onClose }: { years: K1TrackerYearSummary[]; selectedYear?: number; onClose: () => void }) { const [chosen, setChosen] = useState<number[]>(selectedYear ? [selectedYear] : years.slice(-1).map((year) => year.taxYear)); const displayed = useMemo(() => years.filter((year) => chosen.includes(year.taxYear)), [chosen, years]); return <div className="fixed inset-0 z-50 flex justify-end bg-gray-950/40" role="dialog" aria-modal="true" aria-label="Compare tracker years"><div className="h-full w-full max-w-4xl overflow-y-auto bg-white shadow-2xl"><div className="sticky top-0 flex items-center justify-between border-b border-gray-200 bg-white px-5 py-4"><div><h2 className="text-lg font-semibold text-gray-950">Compare years</h2><p className="text-sm text-gray-500">Choose up to three years.</p></div><button type="button" onClick={onClose} aria-label="Close year comparison" className="rounded-md p-2 text-gray-500 hover:bg-gray-100"><X className="h-5 w-5" /></button></div><div className="p-5"><fieldset><legend className="text-sm font-medium text-gray-800">Years</legend><div className="mt-2 flex flex-wrap gap-2">{years.map((year) => { const active = chosen.includes(year.taxYear); return <button key={year.taxYear} type="button" aria-pressed={active} onClick={() => setChosen((current) => active ? current.filter((item) => item !== year.taxYear) : current.length === 3 ? current : [...current, year.taxYear])} className={`rounded-md border px-3 py-2 text-sm ${active ? 'border-atlas-gold bg-amber-50' : 'border-gray-300'}`}>{year.taxYear}</button> })}</div></fieldset><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[36rem] text-left text-sm"><thead className="sticky top-[4.4rem] bg-white text-xs uppercase text-gray-500"><tr><th className="p-3">Metric</th>{displayed.map((year) => <th key={year.taxYear} className="p-3">{year.taxYear}</th>)}</tr></thead><tbody>{[['Ending outside basis', (year: K1TrackerYearSummary) => currency(year.endingOutsideBasis)], ['Suspended loss', (year: K1TrackerYearSummary) => currency(year.cumulativeSuspendedLoss)], ['Excess distribution', (year: K1TrackerYearSummary) => currency(year.taxableExcessDistribution)], ['Section L difference', (year: K1TrackerYearSummary) => currency(year.sectionLDifference)], ['Warnings', (year: K1TrackerYearSummary) => String(year.warningCount)]].map(([label, render]) => <tr key={String(label)} className="border-t border-gray-100"><th className="sticky left-0 bg-white p-3 font-medium text-gray-700">{String(label)}</th>{displayed.map((year) => <td key={year.taxYear} className="p-3">{(render as (year: K1TrackerYearSummary) => string)(year)}</td>)}</tr>)}</tbody></table></div></div></div></div> }
