import { Search } from 'lucide-react'
import type { K1TrackerPartnershipSummary } from '../../../../../packages/types/src/k1-tracker'

const currency = (value: string | null) => value == null ? '—' : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 0, maximumFractionDigits: 0 }).format(Number(value))
export function PartnershipPicker({ items, search, selectedId, loading, error, onSearch, onSelect }: { items: K1TrackerPartnershipSummary[]; search: string; selectedId?: string; loading: boolean; error?: string; onSearch: (value: string) => void; onSelect: (item: K1TrackerPartnershipSummary) => void }) {
  return <aside aria-label="Partnership selector" className="rounded-xl border border-gray-200 bg-white shadow-sm xl:sticky xl:top-0 xl:h-[calc(100vh-10rem)] xl:overflow-auto"><div className="border-b border-gray-100 p-3"><label className="relative block"><Search className="pointer-events-none absolute left-3 top-2.5 h-4 w-4 text-gray-400" /><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Find a partnership" className="w-full rounded-lg border border-gray-300 py-2 pl-9 pr-3 text-sm focus:outline-none focus:ring-2 focus:ring-jackson-gold" /></label></div>{loading ? <p className="p-5 text-sm text-gray-500">Loading partnerships…</p> : error ? <p className="p-5 text-sm text-red-700">{error}</p> : <div className="p-2">{items.map((item) => <button key={item.partnershipId} type="button" onClick={() => onSelect(item)} className={`w-full rounded-lg p-3 text-left ${selectedId === item.partnershipId ? 'bg-amber-50 ring-1 ring-jackson-gold' : 'hover:bg-gray-50'}`}><p className="truncate text-sm font-semibold text-gray-950">{item.partnershipName}</p><p className="mt-1 text-xs text-gray-500">{item.entityName} · {item.yearCount} year{item.yearCount === 1 ? '' : 's'}</p><div className="mt-2 flex items-center justify-between text-xs"><span className={item.warningCount ? 'text-amber-700' : 'text-emerald-700'}>{item.warningCount ? `${item.warningCount} attention items` : 'Up to date'}</span><span className="font-medium text-gray-700">{currency(item.latestEndingOutsideBasis)}</span></div></button>)}{items.length === 0 && <p className="p-4 text-sm text-gray-500">{search ? 'No partnerships match this search.' : 'No tracker partnerships are available.'}</p>}</div>}</aside>
}
